/* import { auth } from "@/auth";
import { User } from "@/model/user-model"; // путь к файлу модели пользователя

const CurrentUser = async () => {
  const session = await auth();

  return (
    <>
      <p>{session ? session.user.email : "email"}</p>
    </>
  );
};

export default CurrentUser;
 */
